<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="by.gto.test.jackson.mybatis.TestMappers">
    <insert id="saveOg">
            insert into test.og (x, dateOnly, localDate, localDateTime, message, guid, dateWithTime)
            VALUES (#{x}, #{dateOnly}, #{localDate}, #{localDateTime}, #{message}, #{guid}, #{dateWithTime});
    </insert>
</mapper>
